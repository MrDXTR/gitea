steps:
- block: Request release
  fields:
  - default: ALL
    key: region
    options:
    - label: us-west3
      value: us-west3
    - label: us-east-2
      value: us-east-2
    - label: ALL
      value: ALL
    prompt: Please select a region for deployment
    select: Region
- command: buildkite-agent pipeline upload .buildkite/workflows/deploy-sad-$(buildkite-agent
    meta-data get region).yml
  label: Deploy sad environment
